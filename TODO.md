# TODO

- [ ] Update hero section in `index.html`:
  - [ ] Wrap “View Projects” and “Download Resume” buttons in a flex container (`.hero-actions`).
  - [ ] Add CSS for `.hero-actions` to align them on the same line with spacing.
  - [ ] Remove inline `margin-left` on the resume download link.
  - [ ] Fix any hero markup closing-tag issues affected by the buttons block.
- [x] Manually verify in browser that both buttons render inline with spacing.


